# ===   Pointage — Frontend Mobile (PWA) ===

# URL du backend API Render (ex: https://topointage-api.onrender.com)
# En local, laisser vide pour utiliser le proxy Vite (localhost:8000)
VITE_API_URL=

# Base path : "/" sur Render Static Site, "/mobile/" en local avec le backend
VITE_BASE_PATH=/
